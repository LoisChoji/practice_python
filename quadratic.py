def quadratic_formula(a, b, c):
    formula = (-b + ( b**2 - (4*a*c))//(2*a))  #(-b - ( b**2 - (4*a*c))//(2*a))
    #it only works for one case how will it work for both?
    return (formula)
print(quadratic_formula(1, 2, 5))



# def mul(a,b):
#     formula = a*b
#     return formula
# answer = mul(2,4)
# print(answer)